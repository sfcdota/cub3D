project(cub3D)
cmake_minimum_required(VERSION 3.12)
set(CMAKE_C_STANDARD 99)
#-Wall -Wextra -Werror	#-Wall -Wextra -Werror	
set(CMAKE_C_FLAGS "-D BUFFER_SIZE=128 -L../libft/ -lft  -L../minilibx -lmlx -framework OpenGL -framework AppKit")
#set(CMAKE_C_FLAGS "-D BUFFER_SIZE=128 -framework OpenGL -framework AppKit")	
set(ENV{DYLD_LIBRARY_PATH} "../minilibx/")
file(GLOB srcs "*.c")
#add_executable(cub3D ${srcs} cub3D.h)	
file(GLOB gnl "./get_next_line/*.c")
file(GLOB raycast "./raycasting/*.c")
file(GLOB parser "./parser/*.c")
add_executable(cub3D ${srcs} ${gnl} ${raycast} ${parser})
include_directories(libft/)
include_directories(get_next_line/)
include_directories(minilibx/)
include_directories(/usr/local/include)
link_directories(libft)
link_directories(minilibx)
target_link_libraries(cub3D LINK_PUBLIC ft mlx)